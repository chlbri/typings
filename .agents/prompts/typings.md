# Write a .md solution inside @.claude/solutions/ for typings.

I want a cli helper to type machine, it uses too much typescript server.

NB : Read all necessary files in a batch, to write the .md file, and write
the .md file in one go.

## Rule 1 : Each machine file will be name like "_.fsm.ts" or "_.machine.ts", only one machine that is export default.

## Rule 2 : createMachine now will have 3 args

The first one will be the relative name to the project, generated by the
cli system on createion of file and be watched (use npm lib chokidar).

## Rule 3: We will have a big object call MACHINES that carry all created machines.

We will call each machine by a function "getMachine('relativePath')".

## Rule 4: the createMachine will optimizes the config alongside the user write.

## Rule 5: the third argument will be handled.

If no events, not actors the third arg will be optional (because you can
only type context, and pContext) and the thir arg will be type as partial
only with props context and pContext.

## Rule 6 : Now the actors inside third arg will regroup chidren and emitters inside the same object

We will differentiate them by their props.

## Rule 7 : the third arg will use typings helper inside ./src/utils/typings.ts to generate the types of context, pContext, actors, events.
