{
  "name": "Node.js Library",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:24-bookworm",
  // 8 GB RAM for the container
  "runArgs": ["--memory=8g", "--memory-swap=8g", "--cpus=3"],
  "hostRequirements": {
    "memory": "8gb"
  },
  // pnpm via corepack (ships with Node 24) + install deps
  "postCreateCommand": "corepack use pnpm@latest && pnpm run ci:admin",
  // Forward vitest --ui port, also Ollama
  "forwardPorts": [51204, 11434],
  "portsAttributes": {
    "51204": {
      "label": "Vitest UI",
      "onAutoForward": "notify"
    },
    "11434": {
      "label": "Ollama",
      "onAutoForward": "notify"
    }
  },
  "remoteEnv": {
    "NODE_ENV": "development",
    // Give Node.js ~6 GB for heap
    "NODE_OPTIONS": "--max-old-space-size=6144",
    // pnpm store inside the container (no cross-device issues)
    "PNPM_HOME": "/home/node/.local/share/pnpm"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "oxc.oxc-vscode",
        "coverage-gutters.coverageFileNames": [".coverage/lcov.info"],
        "vitest.enable": true,
        "vitest.commandLine": "pnpm run test:only"
      },
      "extensions": [
        "ms-vscode.vscode-typescript-next",
        "ms-vscode.js-debug",
        "oxc.oxc-vscode",
        "bokuweb.vscode-ripgrep",
        "vitest.explorer",
        "ryanluker.vscode-coverage-gutters",
        "adam-bender.commit-message-editor",
        "github.vscode-github-actions",
        "github.copilot",
        "github.copilot-chat",
        "anthropic.claude-code",
        "Gruntfuggly.todo-tree",
        "TypeScriptTeam.native-preview",
        "rioj7.commandOnAllFiles",
        "formulahendry.code-runner",
        "ms-azuretools.vscode-containers",
        "yatki.vscode-surround"
      ]
    }
  }
}
