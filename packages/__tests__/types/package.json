{
  "name": "types",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "_lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "lint": "pnpm run fmt && pnpm run lint:fix",
    "p-q": "pretty-quick",
    "posttest": "exit 0",
    "test:only": "vitest run --no-coverage",
    "_ci": "pnpm run lint && pnpm run test"
  },
  "devDependencies": {
    "@bemedev/dev-utils": "^1.5.0",
    "@bemedev/typings": "workspace:^",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
